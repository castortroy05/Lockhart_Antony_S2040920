<resources>
    <!-- Base application theme using Material Design 3. -->
    <style name="Theme.Lockhart_Antony_S2040920" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/MediumPurple</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorPrimaryContainer">@color/LightSkyBlue</item>
        <item name="colorOnPrimaryContainer">@color/DarkBlue</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/LightSkyBlue</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <!-- Error color. -->
        <item name="colorError">@color/PaleVioletRed</item>

        <!-- Surface colors. -->
        <item name="colorSurface">@android:color/white</item>
        <item name="colorOnSurface">@android:color/black</item>

        <!-- Background colors. -->
        <item name="android:colorBackground">@android:color/white</item>
        <item name="colorOnBackground">@android:color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryContainer</item>
    </style>
</resources>